- name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ubuntu-24.04-docker-packages-$(date +%Y%m%d)
          name: Ubuntu 24.04 Docker Packages $(date +%Y%m%d)
          body: |
            📦 Ubuntu 24.04 (Noble) Docker 组件包：
            
            - containerd.io
            - docker-ce
            - docker-ce-cli
            - docker-buildx-plugin
            - docker-compose-plugin
            
            包含架构: amd64
          files: |
            ${{ github.workspace }}/${{ env.TARGET_DIR }}/deps.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GH_RELEASE_TOKEN }}  # 使用新 Secret
