name: Download Docker & Ubuntu System Packages

on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *' # 每天 02:00 UTC 自动运行

env:
  UBUNTU_CODENAME: noble   # 固定为 Ubuntu 24.04
  TARGET_DIR: deps         # 统一解压目标目录
  ARCH: amd64              # 固定架构为 amd64
  VERSION_FILE: version_info.txt # 版本信息文件

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends curl jq gzip

      # ----------------------------
      # Step 1: Prepare Directories
      # ----------------------------
      - name: Create target directory
        run: mkdir -p "$TARGET_DIR"

      # ----------------------------
      # Step 2: Download Docker Components
      # ----------------------------
      - name: Fetch Docker package URLs and versions
        id: docker_info
        run: |
          set -euo pipefail
          BASE_URL="https://download.docker.com/linux/ubuntu/dists/${UBUNTU_CODENAME}/pool/stable/${ARCH}"
          
          # 获取页面内容
          PAGE_CONTENT=$(curl -fsSL "$BASE_URL/" | tr -d '\r')
          
          # 精确提取包文件名和版本的函数
          get_latest() {
            local pkg="$1"
            local matches=$(echo "$PAGE_CONTENT" | grep -oE "<a href=\"${pkg}[^\"]+\">" | cut -d'"' -f2)
            
            if [ -z "$matches" ]; then
              echo "ERROR: No package found for ${pkg}" >&2
              exit 1
            fi
            
            # 按版本号排序并取最新版本
            local latest=$(echo "$matches" | sort -Vr | head -n1)
            
            # 提取版本号（去除文件名前缀和后缀）
            local version=$(echo "$latest" | sed -E "s/^${pkg}_([0-9]+\.[0-9]+\.[0-9]+[^_]+)_.*$/\1/")
            
            echo "$pkg_URL=$BASE_URL/$latest" >> $GITHUB_OUTPUT
            echo "$pkg_VERSION=$version" >> $GITHUB_OUTPUT
          }

          get_latest "containerd.io"
          get_latest "docker-ce"
          get_latest "docker-ce-cli"
          get_latest "docker-buildx-plugin"
          get_latest "docker-compose-plugin"

      - name: Download Docker components
        run: |
          set -euo pipefail
          
          # 初始化版本文件
          echo "# 版本信息" > "$VERSION_FILE"
          echo "## Docker 组件" >> "$VERSION_FILE"
          echo "" >> "$VERSION_FILE"

          # 下载每个 Docker 组件
          echo "Downloading containerd.io..."
          curl -fsSL "${{ steps.docker_info.outputs.containerd.io_URL }}" -o "$TARGET_DIR/containerd.io.deb"
          echo "containerd.io: ${{ steps.docker_info.outputs.containerd.io_VERSION }}" >> "$VERSION_FILE"

          echo "Downloading docker-ce..."
          curl -fsSL "${{ steps.docker_info.outputs.docker-ce_URL }}" -o "$TARGET_DIR/docker-ce.deb"
          echo "docker-ce: ${{ steps.docker_info.outputs.docker-ce_VERSION }}" >> "$VERSION_FILE"

          echo "Downloading docker-ce-cli..."
          curl -fsSL "${{ steps.docker_info.outputs.docker-ce-cli_URL }}" -o "$TARGET_DIR/docker-ce-cli.deb"
          echo "docker-ce-cli: ${{ steps.docker_info.outputs.docker-ce-cli_VERSION }}" >> "$VERSION_FILE"

          echo "Downloading docker-buildx-plugin..."
          curl -fsSL "${{ steps.docker_info.outputs.docker-buildx-plugin_URL }}" -o "$TARGET_DIR/docker-buildx-plugin.deb"
          echo "docker-buildx-plugin: ${{ steps.docker_info.outputs.docker-buildx-plugin_VERSION }}" >> "$VERSION_FILE"

          echo "Downloading docker-compose-plugin..."
          curl -fsSL "${{ steps.docker_info.outputs.docker-compose-plugin_URL }}" -o "$TARGET_DIR/docker-compose-plugin.deb"
          echo "docker-compose-plugin: ${{ steps.docker_info.outputs.docker-compose-plugin_VERSION }}" >> "$VERSION_FILE"

      # ----------------------------
      # Step 3: Download Ubuntu System Packages
      # ----------------------------
      - name: Define system packages
        run: |
          echo "SYSTEM_PACKAGES=uuid-runtime iptables libip4tc2 libip6tc2 libnetfilter-conntrack3 libnfnetlink0 libnftnl11 libxtables12" >> $GITHUB_ENV

      - name: Download system packages
        run: |
          set -euo pipefail
          packages=(${SYSTEM_PACKAGES// / })
          
          # 添加系统包标题到版本文件
          echo "" >> "$VERSION_FILE"
          echo "## Ubuntu 系统包" >> "$VERSION_FILE"
          echo "" >> "$VERSION_FILE"
          
          # 遍历所有可能的仓库子目录
          REPOS=("main" "updates" "security")
          for repo in "${REPOS[@]}"; do
            echo "Trying repository: $repo"
            PACKAGES_GZ_URL="https://archive.ubuntu.com/ubuntu/dists/${UBUNTU_CODENAME}/${repo}/binary-${ARCH}/Packages.gz"
            if ! curl -fsSL "$PACKAGES_GZ_URL" -o Packages.gz; then
              echo "WARNING: Failed to download Packages.gz from $repo"
              continue
            fi
            
            if ! gunzip -c Packages.gz > Packages; then
              echo "WARNING: Failed to extract Packages from $repo"
              rm -f Packages.gz
              continue
            fi
            
            # 解析Packages文件并下载对应包
            current_pkg=""
            current_version=""
            current_filename=""
            while IFS= read -r line; do
              if [[ "$line" == "Package: "* ]]; then
                current_pkg=$(echo "$line" | awk '{print $2}')
              elif [[ "$line" == "Version: "* ]]; then
                current_version=$(echo "$line" | awk '{print $2}')
              elif [[ "$line" == "Filename: "* ]]; then
                current_filename=$(echo "$line" | awk '{print $2}')
              elif [[ -z "$line" && -n "$current_pkg" && " ${packages[*]} " =~ " ${current_pkg} " ]]; then
                # 遇到空行，说明一个包的信息块结束
                FILE_URL="https://archive.ubuntu.com/ubuntu/${current_filename}"
                echo "Downloading $current_pkg ($current_version) from $repo..."
                if curl -fsSL "$FILE_URL" -o "$TARGET_DIR/$current_pkg.deb"; then
                  echo "$current_pkg: $current_version" >> "$VERSION_FILE"
                  packages=(${packages[@]/$current_pkg}) # 移除已下载的包
                  echo "Successfully downloaded $current_pkg"
                else
                  echo "WARNING: Failed to download $current_pkg from $repo"
                fi
                # 重置
                current_pkg=""
                current_version=""
                current_filename=""
              fi
            done < Packages
            
            rm -f Packages Packages.gz
            
            if [ ${#packages[@]} -eq 0 ]; then
              echo "All system packages downloaded successfully!"
              break
            fi
          done

          if [ ${#packages[@]} -ne 0 ]; then
            echo "ERROR: Missing packages: ${packages[@]}" >&2
            exit 1
          fi

      # ----------------------------
      # Step 4: Verify and Compress
      # ----------------------------
      - name: List files in target directory
        run: ls -la "$TARGET_DIR"

      - name: Compress deps directory
        run: |
          cd "$TARGET_DIR"
          tar -czvf deps.tar.gz ./*
          echo "Final deps.tar.gz structure:"
          tar -tf deps.tar.gz

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ubuntu-24.04-docker-system-deps
          path: "$TARGET_DIR/deps.tar.gz"

      # ----------------------------
      # Step 5: Create GitHub Release
      # ----------------------------
      - name: Generate release tag
        id: generate_tag
        run: |
          RELEASE_TAG="ubuntu-24.04-deps-$(date +'%Y%m%d')"
          echo "RELEASE_TAG=$RELEASE_TAG" >> $GITHUB_ENV

      - name: Read version info
        id: read_version_info
        run: |
          VERSION_INFO=$(cat "$VERSION_FILE")
          VERSION_INFO_ESCAPED=$(echo "$VERSION_INFO" | jq -s -R .)
          echo "VERSION_INFO=$VERSION_INFO_ESCAPED" >> $GITHUB_ENV

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: Ubuntu 24.04 Docker + System Dependencies (${{ env.RELEASE_TAG }})
          body: |
            📦 包含以下组件：
            
            ${{ fromJSON(env.VERSION_INFO) }}
            
            ---
            
            **支持架构**: amd64  
            **工作流**: ${{ github.workflow }}
          files: |
            ${{ github.workspace }}/${{ env.TARGET_DIR }}/deps.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GH_RELEASE_TOKEN }}

      # ----------------------------
      # Step 6: Cleanup
      # ----------------------------
      - name: Cleanup
        run: |
          rm -rf "$TARGET_DIR" "$VERSION_FILE"
